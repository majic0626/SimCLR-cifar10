cifar10 folder is just an example showing how to organize directory structure.

  1. cifar10 contains 2 folers(train/test) and 1 file(class.txt)
  2. train is composed of 10(2 for example here) folder named by different classes.
  3. each "class folder" includes raw images (1 for example here) belong to the class.
  
  cifar10
        |train
             |cat
                 |xxx.jpg
                 |xxx.jpg
             |dog
              .
              .
    
        |test
        |class.txt